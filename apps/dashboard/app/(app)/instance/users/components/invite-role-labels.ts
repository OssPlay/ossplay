export const INVITE_ROLE_LABELS: Record<"none" | "org_creator" | "root", string> = {
	none: "No instance role",
	org_creator: "Organization creator — can create organizations",
	root: "Instance administrator (root) — full access",
};
